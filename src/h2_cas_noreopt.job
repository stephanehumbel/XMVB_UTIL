#!/bin/sh
# Script genere automatiquement a partir de subgms
#
#$ -N h2_cas_noreopt
#
#SBATCH --job-name=h2_cas_noreopt
#SBATCH --cpus-per-task=8
#SBATCH --mem=3000
#***************************
#DO NOT MODIFY THESE OPTIONS
#SBATCH --nodes=1
#SBATCH --ntasks=1
#***************************


#$ -cwd
#$ -S /bin/sh
 hhomedir=`pwd`
 nnode=`hostname`
echo "--------SLURM EXECUTION------ -",date
echo "Variables"
echo "Running on        : $nnode  "
echo "targetdir         : $hhomedir "
echo "VBDIR            : /home/humbel/xmvb3_2/"
echo "TMPDIR            : $TMPDIR"
echo "I am in           : $PWD"
#echo "SLURM_JOB_ID :  ${SLURM_JOB_ID}"

# source /usr/share/Modules/init/sh
# save previous run
if [ -s h2_cas_noreopt.orb ] ; then
 set  echo
   cp h2_cas_noreopt.orb h2_cas_noreopt.orb~
 unset  echo
fi
if [ -s h2_cas_noreopt.xmo ] ; then
 set  echo
   cp h2_cas_noreopt.xmo h2_cas_noreopt.xmo~
 unset  echo
fi
if [ -s h2_cas_noreopt.log ] ; then
 set  echo
   cp h2_cas_noreopt.log h2_cas_noreopt.log~
 unset  echo
fi

set echo
export OMP_NUM_THREADS=8
export OMP_STACKSIZE=3000M
cp  $HOMEDIR/h2_cas_noreopt.xmi  $TMPDIR/h2_cas_noreopt.xmi
if [ -s h2_cas_noreopt.gus ] ; then
    cp  $HOMEDIR/h2_cas_noreopt.gus  $TMPDIR/h2_cas_noreopt.gus
else 
# use .orb as a .gus
    cp  $HOMEDIR/h2_cas_noreopt.orb  $TMPDIR/h2_cas_noreopt.gus
fi

cd $TMPDIR 
#time (/home/humbel/xmvb3_2//bin/preint                 h2_cas_noreopt                      )
#pwd;ls -lrt |head -3
time (/home/humbel/xmvb3_2//bin/xmvb                   h2_cas_noreopt                      )
pwd;ls -lrt |head -3
cd $HOMEDIR 
#time (rungms_                     h2_cas_noreopt  1  > h2_cas_noreopt.log)
# On doit rappatrier à la main le .xmo et les wfn (si necessaire)
cp  $TMPDIR/h2_cas_noreopt.orb  $HOMEDIR/
cp  $TMPDIR/h2_cas_noreopt.xmo  $HOMEDIR/
cp  $TMPDIR/h2_cas_noreopt.xdat  $HOMEDIR/
# join slurm and xmo
cat slurm-${SLURM_JOB_ID}.out h2_cas_noreopt.xmo >> h2_cas_noreopt.xmo
unset echo
