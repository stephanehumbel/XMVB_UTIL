#!/bin/sh
# Script genere automatiquement a partir de subgms
#
#$ -N h2_bovb3
#
#SBATCH --job-name=h2_bovb3
#SBATCH --cpus-per-task=8
#SBATCH --mem=3000
#***************************
#DO NOT MODIFY THESE OPTIONS
#SBATCH --nodes=1
#SBATCH --ntasks=1
#***************************


#$ -cwd
#$ -S /bin/sh
 hhomedir=`pwd`
 nnode=`hostname`
echo "--------SLURM EXECUTION------ -",date
echo "Variables"
echo "Running on        : $nnode  "
echo "targetdir         : $hhomedir "
echo "VBDIR            : /home/humbel/xmvb3_2/"
echo "TMPDIR            : $TMPDIR"
echo "I am in           : $PWD"
#echo "SLURM_JOB_ID :  ${SLURM_JOB_ID}"

# source /usr/share/Modules/init/sh
# save previous run
if [ -s h2_bovb3.orb ] ; then
 set  echo
   cp h2_bovb3.orb h2_bovb3.orb~
 unset  echo
fi
if [ -s h2_bovb3.xmo ] ; then
 set  echo
   cp h2_bovb3.xmo h2_bovb3.xmo~
 unset  echo
fi
if [ -s h2_bovb3.log ] ; then
 set  echo
   cp h2_bovb3.log h2_bovb3.log~
 unset  echo
fi

set echo
export OMP_NUM_THREADS=8
export OMP_STACKSIZE=3000M
cp  $HOMEDIR/h2_bovb3.xmi  $TMPDIR/h2_bovb3.xmi
if [ -s h2_bovb3.gus ] ; then
    cp  $HOMEDIR/h2_bovb3.gus  $TMPDIR/h2_bovb3.gus
else 
# use .orb as a .gus
    cp  $HOMEDIR/h2_bovb3.orb  $TMPDIR/h2_bovb3.gus
fi

cd $TMPDIR 
#time (/home/humbel/xmvb3_2//bin/preint                 h2_bovb3                      )
#pwd;ls -lrt |head -3
time (/home/humbel/xmvb3_2//bin/xmvb                   h2_bovb3                      )
pwd;ls -lrt |head -3
cd $HOMEDIR 
#time (rungms_                     h2_bovb3  1  > h2_bovb3.log)
# On doit rappatrier à la main le .xmo et les wfn (si necessaire)
cp  $TMPDIR/h2_bovb3.orb  $HOMEDIR/
cp  $TMPDIR/h2_bovb3.xmo  $HOMEDIR/
cp  $TMPDIR/h2_bovb3.xdat  $HOMEDIR/
# join slurm and xmo
cat slurm-${SLURM_JOB_ID}.out h2_bovb3.xmo >> h2_bovb3.xmo
unset echo
