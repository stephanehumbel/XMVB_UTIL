#!/bin/sh
# Script genere automatiquement a partir de subgms
#
#$ -N b_d_cas
#
#SBATCH --job-name=b_d_cas
#SBATCH --cpus-per-task=8
#SBATCH --mem=3000
#***************************
#DO NOT MODIFY THESE OPTIONS
#SBATCH --nodes=1
#SBATCH --ntasks=1
#***************************


#$ -cwd
#$ -S /bin/sh
 hhomedir=`pwd`
 nnode=`hostname`
echo "--------SLURM EXECUTION------ -",date
echo "Variables"
echo "Running on        : $nnode  "
echo "targetdir         : $hhomedir "
echo "VBDIR            : /home/humbel/xmvb3_2/"
echo "TMPDIR            : $TMPDIR"
echo "I am in           : $PWD"
#echo "SLURM_JOB_ID :  ${SLURM_JOB_ID}"

# source /usr/share/Modules/init/sh
# save previous run
if [ -s b_d_cas.orb ] ; then
 set  echo
   cp b_d_cas.orb b_d_cas.orb~
 unset  echo
fi
if [ -s b_d_cas.xmo ] ; then
 set  echo
   cp b_d_cas.xmo b_d_cas.xmo~
 unset  echo
fi
if [ -s b_d_cas.log ] ; then
 set  echo
   cp b_d_cas.log b_d_cas.log~
 unset  echo
fi

set echo
export OMP_NUM_THREADS=8
export OMP_STACKSIZE=3000M
cp  $HOMEDIR/b_d_cas.xmi  $TMPDIR/b_d_cas.xmi
if [ -s b_d_cas.gus ] ; then
    cp  $HOMEDIR/b_d_cas.gus  $TMPDIR/b_d_cas.gus
else 
# use .orb as a .gus
    cp  $HOMEDIR/b_d_cas.orb  $TMPDIR/b_d_cas.gus
fi

cd $TMPDIR 
#time (/home/humbel/xmvb3_2//bin/preint                 b_d_cas                      )
#pwd;ls -lrt |head -3
time (/home/humbel/xmvb3_2//bin/xmvb                   b_d_cas                      )
pwd;ls -lrt |head -3
cd $HOMEDIR 
#time (rungms_                     b_d_cas  1  > b_d_cas.log)
# On doit rappatrier à la main le .xmo et les wfn (si necessaire)
cp  $TMPDIR/b_d_cas.orb  $HOMEDIR/
cp  $TMPDIR/b_d_cas.xmo  $HOMEDIR/
cp  $TMPDIR/b_d_cas.xdat  $HOMEDIR/
# join slurm and xmo
cat slurm-${SLURM_JOB_ID}.out b_d_cas.xmo >> b_d_cas.xmo
unset echo
