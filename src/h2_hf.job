#!/bin/sh
# Script genere automatiquement a partir de subgms
#
#$ -N h2_hf
#
#SBATCH --job-name=h2_hf
#SBATCH --cpus-per-task=8
#SBATCH --mem=3000
#***************************
#DO NOT MODIFY THESE OPTIONS
#SBATCH --nodes=1
#SBATCH --ntasks=1
#***************************


#$ -cwd
#$ -S /bin/sh
 hhomedir=`pwd`
 nnode=`hostname`
echo "--------SLURM EXECUTION------ -",date
echo "Variables"
echo "Running on        : $nnode  "
echo "targetdir         : $hhomedir "
echo "VBDIR            : /home/humbel/xmvb3_2/"
echo "TMPDIR            : $TMPDIR"
echo "I am in           : $PWD"
#echo "SLURM_JOB_ID :  ${SLURM_JOB_ID}"

# source /usr/share/Modules/init/sh
# save previous run
if [ -s h2_hf.orb ] ; then
 set  echo
   cp h2_hf.orb h2_hf.orb~
 unset  echo
fi
if [ -s h2_hf.xmo ] ; then
 set  echo
   cp h2_hf.xmo h2_hf.xmo~
 unset  echo
fi
if [ -s h2_hf.log ] ; then
 set  echo
   cp h2_hf.log h2_hf.log~
 unset  echo
fi

set echo
export OMP_NUM_THREADS=8
export OMP_STACKSIZE=3000M
cp  $HOMEDIR/h2_hf.xmi  $TMPDIR/h2_hf.xmi
if [ -s h2_hf.gus ] ; then
    cp  $HOMEDIR/h2_hf.gus  $TMPDIR/h2_hf.gus
else 
# use .orb as a .gus
    cp  $HOMEDIR/h2_hf.orb  $TMPDIR/h2_hf.gus
fi

cd $TMPDIR 
#time (/home/humbel/xmvb3_2//bin/preint                 h2_hf                      )
#pwd;ls -lrt |head -3
time (/home/humbel/xmvb3_2//bin/xmvb                   h2_hf                      )
pwd;ls -lrt |head -3
cd $HOMEDIR 
#time (rungms_                     h2_hf  1  > h2_hf.log)
# On doit rappatrier à la main le .xmo et les wfn (si necessaire)
cp  $TMPDIR/h2_hf.orb  $HOMEDIR/
cp  $TMPDIR/h2_hf.xmo  $HOMEDIR/
cp  $TMPDIR/h2_hf.xdat  $HOMEDIR/
# join slurm and xmo
cat slurm-${SLURM_JOB_ID}.out h2_hf.xmo >> h2_hf.xmo
unset echo
